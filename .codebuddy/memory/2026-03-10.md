# 2026-03-10 工作日志

## questify-mobile 接入 Supabase 后端

### 完成的工作

1. **安装 Supabase SDK**
   - 安装 `@supabase/supabase-js`、`react-native-url-polyfill`、`@react-native-async-storage/async-storage`

2. **创建 Supabase 配置**
   - `.env` 文件：存储 Supabase URL 和 ANON_KEY
   - `src/lib/supabase.ts`：Supabase 客户端配置（单例模式）
   - `src/lib/auth.tsx`：AuthProvider 和 useAuth Hook
   - `src/lib/services.ts`：数据服务层（characterService, questService, achievementService）

3. **更新页面接入真实数据**
   - `app/_layout.tsx`：集成 AuthProvider
   - `app/login.tsx`：实现邮箱注册/登录功能
   - `app/(tabs)/index.tsx`：首页接入真实数据
   - `app/(tabs)/quests.tsx`：任务页面接入真实数据 + 创建任务弹窗
   - `app/(tabs)/character.tsx`：角色页面接入真实数据 + 登出功能
   - `app/(tabs)/achievements.tsx`：成就页面接入真实数据

4. **功能特性**
   - 用户认证（邮箱注册/登录）
   - 游客模式（跳过登录可直接使用，但数据不保存）
   - 任务 CRUD
   - 完成任务获得奖励（经验、金币、属性）
   - 角色数据同步
   - 成就系统

### 数据库表结构（Supabase）

| 表名 | 说明 |
|------|------|
| User | 用户基本信息 |
| Character | 角色数据（等级、经验、属性等）|
| Quest | 任务数据 |
| Achievement | 成就定义 |
| UserAchievement | 用户成就进度 |

### 连接信息

- Supabase URL: `https://wqrjhcqcawemxohwncxz.supabase.co`
- 环境变量使用 `EXPO_PUBLIC_` 前缀（Expo 要求）

---

## Bug 修复

### 1. Web 端 Supabase 存储报错 "window is not defined"

- **文件**: `src/lib/supabase.ts`
- **修复**: 自定义存储适配器，Web 用 localStorage，Native 延迟导入 AsyncStorage

### 2. 创建任务按钮点击无反应 / 点击关闭弹窗

- **文件**: `app/(tabs)/quests.tsx`
- **根因**: Modal 中 Pressable 嵌套导致 Web 端事件冒泡问题
- **修复**: 将 modalOverlay 改为 View + 独立 TouchableOpacity 背景层分层架构，内容层与背景层为兄弟节点

### 3. 创建任务功能无法正常工作

- **文件**: `src/lib/services.ts`、`app/(tabs)/quests.tsx`
- **根因**: 
  1. `questService.create` 缺少 `updatedAt` 字段（数据库 NOT NULL 约束）
  2. Web 端 `Alert.alert` 不弹出提示，用户不知道未登录
- **修复**: 
  1. insert 时添加 `updatedAt: now`
  2. 使用 `Platform.OS === 'web' ? window.alert() : Alert.alert()` 兼容方案

### 4. 注册时 User 表插入 400 Bad Request

- **文件**: `src/lib/auth.tsx`、`app/login.tsx`
- **根因**:
  1. `signUp` 中插入 `User` 记录缺少 `updatedAt` 字段（NOT NULL 无默认值）
  2. Web 端 `Alert.alert` 错误信息不弹出，用户不知道失败原因
  3. 多次重试触发 Supabase auth 429 速率限制
- **修复**:
  1. insert 时添加 `updatedAt: now`
  2. 数据库迁移：为 `User.updatedAt` 列添加 `DEFAULT CURRENT_TIMESTAMP`
  3. `login.tsx` 中使用 Web 兼容的 `showAlert` 函数
  4. 增加 429 速率限制的友好错误提示

### 5. 登录方式改为邮箱验证码（OTP）

- **文件**: `src/lib/auth.tsx`、`app/login.tsx`
- **改动**:
  1. `auth.tsx` 新增 `sendOtp` 和 `verifyOtp` 方法，使用 Supabase `signInWithOtp` + `verifyOtp`
  2. `login.tsx` 完全重写为两步流程：
     - 第1步：输入邮箱 + 角色名（可选）→ 获取验证码
     - 第2步：输入 6 位验证码 → 验证并登录
  3. 特性：60 秒倒计时重发、粘贴验证码自动填充、退格键自动回退、填满自动提交
  4. 无需注册，新用户输入邮箱自动创建账号（`shouldCreateUser: true`）
  5. 去除了密码相关所有逻辑

### 6. 支持 Magic Link 登录 + OTP 验证码双模式

- **文件**: `src/lib/auth.tsx`、`app/login.tsx`
- **改动**:
  1. `auth.tsx` 在 useEffect 中添加 URL hash 解析，自动处理 Magic Link 回调登录
  2. `login.tsx` OTP 页面增加提示文案："点击邮件中的链接直接登录，或输入 6 位验证码"
  3. 兼容 Supabase 默认的 Magic Link 模式（无需修改邮件模板）
- **说明**: 
  - Supabase 默认发送 Magic Link 而非 6 位验证码
  - 如需发送 6 位验证码，需在 Supabase Dashboard → Authentication → Email Templates 中将 `{{ .ConfirmationURL }}` 替换为 `{{ .Token }}`

### 7. 改为本地存储模式（无需登录）

- **文件**: `src/lib/local-storage.ts`（新增）、所有页面文件
- **改动**:
  1. 新建 `local-storage.ts`，使用 AsyncStorage 实现完整的本地数据服务
     - `localCharacterService`: 角色数据（等级、经验、属性、金币）
     - `localQuestService`: 任务 CRUD + 完成追踪
     - `localAchievementService`: 成就进度自动更新
  2. 改造所有页面（index.tsx, quests.tsx, character.tsx, achievements.tsx）
     - 移除 `useAuth` 和 `user` 依赖
     - 使用本地存储服务替代 Supabase 服务
  3. 简化 `_layout.tsx`，移除 AuthProvider
  4. 应用直接进入主页，无需登录
- **特性**:
  - 完成任务自动更新成就进度（任务数、金币、连续天数等）
  - 升级自动更新称号
  - 角色页面可重置所有数据
  - 数据持久化在本地，关闭应用不丢失

### 8. 清理邮件登录相关代码

- **删除的文件**:
  - `app/login.tsx` — 登录页面
  - `src/lib/auth.tsx` — 认证模块（AuthProvider, useAuth）
  - `src/lib/supabase.ts` — Supabase 客户端配置
  - `src/lib/services.ts` — Supabase 数据服务
  - `.env` — Supabase 环境变量
- **修改的文件**:
  - `app/_layout.tsx` — 移除 login 路由
  - `package.json` — 移除 `@supabase/supabase-js`、`react-native-url-polyfill` 依赖
- **当前架构**:
  - 应用完全本地化，无 Supabase 依赖
  - 数据存储使用 AsyncStorage（`src/lib/local-storage.ts`）
  - 打开即用，无需任何登录

### 9. 产品优化：底部 Tab 重构

- **需求**: 将 4 个 Tab 改为 3 个：任务、成就、我的
- **删除的文件**:
  - `app/(tabs)/quests.tsx` — 原任务面板页面
- **修改的文件**:
  - `app/(tabs)/index.tsx` — 重写为任务 Tab，整合原首页和任务面板功能
    - 今日进度（已完成/总任务/完成率）
    - 今日主线任务
    - 其他任务
    - 悬浮添加任务按钮
    - 创建任务弹窗
  - `app/(tabs)/_layout.tsx` — 更新 Tab 布局
    - Tab 顺序：任务、成就、我的
    - "角色" 改名为 "我的"
    - 删除 quests Tab 配置
- **保持不变**:
  - `app/(tabs)/achievements.tsx` — 成就页面
  - `app/(tabs)/character.tsx` — 角色页面（Tab 标题改为"我的"）

### 10. 新增冒险日志 Tab

- **需求文档**: `.codebuddy/memory/requirements_1.md` — 完整的产品需求文档
- **新增文件**:
  - `app/(tabs)/history.tsx` — 冒险日志 Tab 页面
- **修改文件**:
  - `app/(tabs)/_layout.tsx` — 新增日志 Tab（位于任务和成就之间）
- **Tab 结构**:
  | 顺序 | Tab 名称 | 图标 | 说明 |
  |------|---------|------|------|
  | 1 | 任务 | 📋 list | 今日进度 + 主线/其他任务 |
  | 2 | **日志** | 📜 book | **新增**：冒险记录 |
  | 3 | 成就 | 🏆 trophy | 成就殿堂 |
  | 4 | 我的 | 👤 person | 角色详情 |
- **功能特性**:
  1. **KPI 指标卡**（5个，一行紧凑布局）：经验 / 完成 / 主线 / 达成率 / 连续
  2. **热力图日历**：按月展示，支持月份/年份切换
     - 起始月份：2026年2月
     - 结束月份：当前月份
     - 颜色深浅 = 经验值
     - 主线完成 = 🏅勋章
     - 今日边框高亮，选中日期使用珊瑚色边框
     - 未来日期置灰不可点击
  3. **月份选择器**：左右箭头切换月份
     - 显示「2026年3月」格式
     - 当前月显示「本月」徽章
     - 到达边界时箭头置灰禁用
  4. **日详情**：页面内展示（非底部抽屉），跟在日历下方
     - 日结算卡：经验/金币/完成数/主线状态
     - 属性成长：力量/智力/专注/活力
     - 主线任务列表
     - 其他任务列表
  5. **空状态**：无数据时显示引导文案
- **技术方案**:
  - 数据聚合在前端完成（从 localQuestService.getAll() 获取所有任务）
  - 按天筛选已完成任务（status === 'DONE' && completedAt 在指定日期）
  - 热力图颜色映射：0 EXP = 灰色，1-50 = 薄荷1，51-100 = 薄荷2，101-150 = 薄荷3，150+ = 薄荷4
  - 日历按周组织，自动补齐首尾空白格子
  - KPI 统计范围为当前选中月份
- **视觉统一**：所有 emoji 已替换为 Ionicons 图标
  | 原 Emoji | Ionicons 图标 | 用途 |
  |----------|---------------|------|
  | 📜 | book-outline | 冒险日志标题 |
  | ⭐ | star | 经验/主线任务 |
  | ✅ | checkmark-circle | 完成数 |
  | 🎯 | flag | 主线天数 |
  | 📊 | stats-chart | 达成率 |
  | 🔥 | flame | 连续天数 |
  | 📅 | calendar | 日期/日历 |
  | 🏅 | medal | 主线达成勋章 |
  | 💪 | fitness | 属性成长 |
  | ☑️ | checkbox | 已完成任务 |
  | 📋 | list | 其他任务 |
  | 💤 | bed | 空状态/休息 |

---

## 「我的」页面功能增强

### 完成的工作

1. **emoji → Ionicons 替换**
   - `character.tsx`：冒险统计、属性详情、称号历程所有 emoji 替换为统一 Ionicons
   - `CharacterCard.tsx`：顶部装饰、金币、属性 emoji 替换为 Ionicons
   
   | 原 Emoji | Ionicons 图标 | 用途 |
   |----------|---------------|------|
   | ✨⭐ | sparkles, star | 顶部装饰 |
   | 📱 | phone-portrait-outline | 本地存储提示 |
   | 📊 | stats-chart | 冒险统计标题 |
   | 🎯 | flag | 总任务数 |
   | ✅ | checkmark-circle | 已完成 |
   | 🔥 | flame | 连续天数 |
   | ⭐ | star | 总经验 |
   | 💪 | fitness | 属性详情标题 |
   | 💪 | flash | 力量属性 |
   | 🧠 | bulb | 智力属性 |
   | 🎯 | eye | 专注属性 |
   | ❤️ | heart | 活力属性 |
   | 🏅 | medal | 称号历程/勋章 |
   | 🔒 | lock-closed | 未解锁称号 |
   | 💰 | cash | 金币 |
   | 😊 | person | 默认头像 |

2. **编辑昵称功能**
   - 点击角色名称右侧的铅笔图标打开弹窗
   - 弹窗内输入新昵称，支持最多 20 字符
   - 保存后即时更新角色数据

3. **上传头像功能**
   - 安装 `expo-image-picker` 依赖
   - 点击头像触发相册选择（带裁剪，1:1 比例）
   - 头像右上角显示相机图标提示可编辑
   - 新增 `avatarUri` 字段存储用户上传的头像 URI
   - 若无自定义头像，显示默认 person 图标

4. **移除头像动效**
   - 原来使用 `react-native-reanimated` 的弹跳动画已移除
   - 头像现在静态显示，视觉更稳定
